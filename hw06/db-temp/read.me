Этот каталог нужен, чтобы сюда сохранялись файлы H2.
Если оставить H2 в режиме in-mem, то невозможно будет подключиться
непосредственно из IntelliJ Idea.

Для подключения используем режим <URL only> со следующими параметрами:

    user: <пусто>
    password: <пусто>
    url: jdbc:h2:file:<путь к папке с ДЗ>/hw05/db-temp/maindb;AUTO_SERVER=TRUE